import { redirect } from "next/navigation";

/**
 * /plans — נתיב היסטורי.
 *
 * העמוד הישן פרסם מסלולים שאינם קיימים יותר ("בסיס ₪149" / "פרו ₪199") ופיצ'רים
 * שהוסרו או שמעולם לא נבנו (ניהול מקדמות, ניהול צוות, חיזוי נטישה, ניתוח עסקי
 * מתקדם). הוא לא היה מקושר משום מקום באפליקציה, אבל נשאר נגיש בכתובת ישירה —
 * כלומר לקוחה משלמת הייתה עלולה לראות מחירים ופיצ'רים שגויים.
 *
 * מקור האמת היחיד למסלולים הוא src/lib/plans.ts, והמסכים שמציגים אותו הם
 * /subscribe (בחירת מסלול) ו-/upgrade (שדרוג לפלטינום). /upgrade מנתב בעצמו
 * לפי מצב המנוי: ללא מסלול → /subscribe, פלטינום או מנהלת → /dashboard.
 */
export default function PlansPage() {
  redirect("/upgrade");
}
